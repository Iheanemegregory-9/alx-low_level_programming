#include "main.h"

/**
 * _isupper- looks for uppercase chars.
 * @c - the char used to check
 * Return: returns 1 if c == uppercase and return 0 if c != uppercase
 */

int _isupper(int c)
{
	return (c >= 'A' && c <= 'Z');
}
