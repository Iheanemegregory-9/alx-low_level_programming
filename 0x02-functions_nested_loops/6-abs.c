#include "main.h"

/**
 * _abs - prints the absolute value of an integrer
 *
 * @n: n is the integer
 *
 * Return: n(the integer)
 */

int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	else if (n == 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
